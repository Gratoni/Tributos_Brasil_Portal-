RewriteEngine On

# Serve arquivos e pastas reais normalmente.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Fallback SPA para React Router.
RewriteRule ^ index.html [L]
